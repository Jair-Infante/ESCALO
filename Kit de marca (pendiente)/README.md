# Kit de marca — pendiente de definir

Todavia no esta definido el kit de marca de ESCALO (colores, logo, tipografia).

Cuando lo definas, esta carpeta deberia tener:

- logo.svg / logo.png (version color y version blanco)
- favicon.ico / favicon.png
- paleta de colores (hex de cada color)
- tipografia elegida

La landing en `../Web (escalo.com.ar)/index.html` ya esta preparada para tomar
esos datos sin rehacer nada: los colores se cambian en 2 lineas del CSS
(variables --accent y --accent-dark al principio del archivo), y el logo se
reemplaza en una sola linea (`<div class="logo">ESCALO</div>`).
